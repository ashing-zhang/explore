# Local environment variables for rl/ scripts.
# These values are safe defaults; adjust paths as needed.

# Config file path (optional). If empty, defaults to configs/default.yaml.
HOTEL_RL_CONFIG=

# Model path for evaluation (optional). If empty, evaluator auto-detects rl/trained_model.zip.
HOTEL_RL_MODEL=

